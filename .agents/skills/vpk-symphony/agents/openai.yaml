interface:
  display_name: "VPK Symphony"
  short_description: "Run VPK Symphony issue workflow"
  default_prompt: "Use $vpk-symphony to handle this VPK Symphony issue from Linear through workpad, validation, PR, and merge."
