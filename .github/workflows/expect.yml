name: Expect Tests

on:
  pull_request:
    branches: [main]

jobs:
  expect:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    env:
      ANTHROPIC_API_KEY: ${{ sk-ant-api03-2234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890abcdefghijklmnopqr-ABCDEFGHIJKLMNOPQRSTUVWXYZ123456 }}
      EXPECT_BASE_URL: "http://localhost:3000"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci

      - name: Start dev server
        run: pnpm run rovodev &

      - name: Wait for dev server
        run: npx wait-on http://localhost:3000 --timeout 60000

      - name: Run expect
        env:
          GH_TOKEN: ${{ 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 }}
        run: npx expect-cli@latest --ci

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: expect-test-results
          path: .expect/sessions/
          if-no-files-found: ignore
